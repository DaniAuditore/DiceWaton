rules:
  - p/owasp-top-ten

# Minimal, auditable exclusions for generated/vendor artifacts only.
paths:
  exclude:
    - dist/**
    - node_modules/**
    - playwright-report/**
    - test-results/**
